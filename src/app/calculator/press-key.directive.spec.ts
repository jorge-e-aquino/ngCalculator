import { PressKeyDirective } from './press-key.directive';

describe('PressKeyDirective', () => {
  it('should create an instance', () => {
    const directive = new PressKeyDirective();
    expect(directive).toBeTruthy();
  });
});
